# Runtime dependencies. Deliberately short: every dependency is attack surface
# and a source of version drift.
requests>=2.31,<3          # the only HTTP client, used solely by RequestsTransport
numpy>=1.24,<3             # indicator math

# Optional. kelly_log_opt uses scipy's bounded minimiser when present and falls
# back to a golden-section search when it is not, so the bot runs without it.
scipy>=1.11,<2

# Test-only (not needed at runtime):
#   pytest>=8

# Optional. Required only to TRAIN or SERVE a learned policy (policy.py,
# tools/train_policy.py). The bot starts and trades without it: `policy` reports
# a permanent "unavailable" state and the classical strategy runs unchanged.
# A missing ML dependency must never stop an execution stack.
scikit-learn>=1.3,<2
